---
name: index
description: Show phasebook index — overview of all tasks across folders.
allowed-tools: Bash
user-invocable: true
---

Run `phasebook index` and display the output to the user.
If the command is not found, tell the user to install phasebook: `pip install -e ~/projects/phasebook`
